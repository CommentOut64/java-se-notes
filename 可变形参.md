在 Java 5 之后，Java 引入了可变参数（Varargs）功能，允许方法接受数量可变的参数，从而提高代码的灵活性和可读性。

**使用方法：**

定义可变参数的方法时，在参数类型后加上省略号（`...`），表示该参数可以接受多个值，这些值在方法内部被当作数组处理。

```java
public class VarargsExample {
    // 定义一个可变参数方法
    public void printNumbers(int... numbers) {
        for (int number : numbers) {
            System.out.println(number);
        }
    }

    public static void main(String[] args) {
        VarargsExample example = new VarargsExample();
        example.printNumbers(1, 2, 3, 4, 5); // 输出 1 2 3 4 5
        example.printNumbers(10, 20);       // 输出 10 20
        example.printNumbers();             // 不输出任何内容
    }
}
```

**注意事项：**

1. **可变参数必须是方法的最后一个参数**：在方法的参数列表中，可变参数必须位于最后，这样才能确保方法调用时参数的正确解析。

    ```java
    // 正确
    public void exampleMethod(String name, int... numbers) { }
    
    // 错误：可变参数不是最后一个参数
    public void exampleMethod(int... numbers, String name) { }
    ```

2. **一个方法中只能有一个可变参数**：由于可变参数必须位于参数列表的末尾，因此一个方法中只能声明一个可变参数。

    ```java
    // 错误：方法中有多个可变参数
    public void exampleMethod(int... numbers, String... strings) { }
    ```

3. **可变参数与方法重载**：如果存在与可变参数方法同名但参数数量固定的方法，调用时会优先匹配参数数量固定的方法。

    ```java
    public class OverloadExample {
        public void display(int num) {
            System.out.println("Single parameter: " + num);
        }
    
        public void display(int... nums) {
            System.out.println("Multiple parameters: " + Arrays.toString(nums));
        }
    
        public static void main(String[] args) {
            OverloadExample example = new OverloadExample();
            example.display(5);        // 调用第一个方法
            example.display(1, 2, 3);  // 调用第二个方法
        }
    }
    ```

    在上述代码中，`example.display(5)` 会调用第一个方法，因为它与单个 `int` 参数匹配更精确。

4. **可变参数与数组参数不构成方法重载**：可变参数在编译时会被转换为数组形式，因此方法签名相同，不能作为方法重载。

    ```java
    // 错误：方法签名相同，不能构成重载
    public void exampleMethod(int... numbers) { }
    
    public void exampleMethod(int[] numbers) { }
    ```

5. **避免与重载方法产生二义性**：在方法重载时，如果可变参数方法与其他方法的参数列表可能产生二义性，编译器会报错。

    ```java
    // 错误：编译器无法确定调用哪个方法
    public void exampleMethod(int... numbers) { }
    
    public void exampleMethod(int number, int... numbers) { }
    ```

通过遵循上述使用方法和注意事项，可以有效地在 Java 中使用可变参数，提高代码的灵活性和可读性。